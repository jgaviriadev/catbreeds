import 'package:flutter/material.dart';

/// Defines the color palette used throughout the application.
/// 
/// This class contains all the base colors for the app theme.
/// Colors are defined as constants to ensure consistency across the UI.
class AppColors {
  // Common colors
  static const primary = Color(0xFF7C69AC);
  static const secondary = Color(0xFF607D8B);
  static const tertiary = Color(0xFF95698A);
  static const error = Color(0xFFFF3B30);

  // Light theme colors
  static const lightBackground = Color(0xFFFDFBF7);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightText = Color(0xFF31332F);

  // Dark theme colors
  static const darkBackground = Color(0xFF000000);
  static const darkSurface = Color(0xFF1C1C1E);
  static const darkText = Color(0xFFFFFFFF);
}
